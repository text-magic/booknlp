# Support Python 3.10+
target-version = "py310"
# Set the msximum line length to 120
line-length = 120

[lint]
# Add the `line-too-long` rule to the enforced rule set.
extend-select = [
    "E501",
    "I", # isort
]
